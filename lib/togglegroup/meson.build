togglegroup_sources = [
  'togglegroup.cpp'
]
togglegroup_headers = include_directories('.')

togglegroup = static_library('togglegroup',
  togglegroup_sources,
  dependencies: gtkdep,
  include_directories: togglegroup_headers,
)
